import 'package:flutter/material.dart';

class Game33Page extends StatelessWidget {
  const Game33Page({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
      body: Container()

    );
  }
}
