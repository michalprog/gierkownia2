import 'package:flutter/material.dart';

class TttGameView extends StatelessWidget {
  const TttGameView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
